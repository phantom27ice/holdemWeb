# 2026年03月18日02时12分12秒 阶段日志

## 本阶段目标

1. 按 `spec` 模式启动项目开发。
2. 完成引擎骨架与静态牌桌（S1）落地。
3. 确保当前代码可正常构建。

## 已完成事项

1. 完成德州引擎基础类型定义与动作协议。
2. 搭建 `holdemEngine` 骨架，包含：
   1. `createInitialHandState`
   2. `dispatch`
   3. `getLegalActions`
3. 建立资源解析层 `resourceResolver`，统一从 `src/resources` 解析图片 URL。
4. 构建 mock 牌局数据与 Pinia `tableStore`，用于驱动静态桌面。
5. 按 `UI_SPEC` 拆分并实现首批牌桌组件：
   1. `TableScene`
   2. `TableShell`
   3. `SeatView`
   4. `BoardCards`
   5. `HoleCards`
   6. `PotBar`
   7. `ActionPanel`
6. 首页已切换为牌桌场景展示。
7. 执行 `npm run build`，构建通过。

## 关键变更文件

1. `src/game/types/index.ts`
2. `src/game/engine/holdemEngine.ts`
3. `src/game/engine/deck.ts`
4. `src/game/assets/resourceResolver.ts`
5. `src/game/mock/createMockTableState.ts`
6. `src/stores/tableStore.ts`
7. `src/components/table/*`
8. `src/views/HomeView.vue`
9. `src/App.vue`
10. `src/style.css`

## 当前阶段结论

1. 项目已从模板页进入“可见牌桌 + 可扩展引擎”的开发状态。
2. 下一阶段可直接进入：
   1. 完整下注流程（真实 `dispatch` 金额流转）
   2. 街道推进（Preflop -> River）
   3. 结算核心（边池、摊牌、分池）

# 2026年03月18日02时36分04秒 阶段日志

## 本阶段目标

1. 按 spec 模式推进 M1 的“可验证”实现。
2. 增加自动化测试并回填 checklist。

## 本阶段完成

1. 引擎升级为可执行 Preflop 闭环：
   1. `START_HAND -> POST_FORCED_BETS -> DEAL_HOLE_CARDS -> BETTING_PRE_FLOP`
   2. Preflop 完成后自动发 Flop 并进入 `BETTING_FLOP`
2. Hero 操作按钮接入真实引擎动作（`FOLD/CHECK/CALL/RAISE`）。
3. 简单 AI 自动行动流程接入，直到轮到 Hero 或当前街结束。
4. 新增自动化测试（Vitest）：
   1. BB option 保留
   2. 最小加注边界
   3. Preflop -> Flop 推进
   4. 仅剩一人时直接 `PAYOUT`
5. `GAME_SPEC.md` 的 M1 Checklist 已回填（第 1-7 项完成）。

## 验证结果

1. `npm test` 通过（4/4）。
2. `npm run build` 通过。

## 待完成

1. M1 Checklist 第 8 项（统一不变量检查）尚未接入引擎执行路径。
2. M2 规则（边池、摊牌比较、分池）尚未开始。

# 2026年03月18日02时49分31秒 阶段日志

## 本阶段目标

1. 完成 Phase A（M1 收口）：非法动作显式错误 + 不变量守卫。
2. 补齐对应自动化测试，确保可回归验证。

## 本阶段完成

1. 引擎结果结构扩展：`EngineResult` 新增 `error` 字段，定义统一 `EngineError` 与错误码。
2. `dispatch` 行为升级：
   1. 执行前校验输入状态不变量。
   2. 执行后校验输出状态不变量。
   3. 校验失败返回 `INVARIANT_VIOLATION` 并阻断状态提交。
3. 玩家动作错误显式化：
   1. 越权行动返回 `NOT_ACTOR_TURN`。
   2. 非法动作返回 `ILLEGAL_ACTION`。
   3. 非法金额返回 `INVALID_AMOUNT`。
   4. 非法阶段返回 `INVALID_PHASE`。
4. Store 接入错误通道：新增 `lastEngineError`，不再吞掉引擎错误。
5. 测试补齐并通过：
   1. 新增 out-of-turn 错误测试。
   2. 新增非法加注金额错误测试。
   3. 新增不变量输入失败测试。
   4. 总测试数由 4 增至 7，全部通过。

## 验证结果

1. `npm test` 通过（7/7）。
2. `npm run build` 通过。

## 阶段结论

1. Phase A 验收目标达成，可进入 Phase B。
2. 下一阶段聚焦：`evaluate7/compareRank` 与无边池 showdown 结算。

# 2026年03月18日02时55分25秒 阶段日志

## 本阶段目标

1. 推进 Phase B：实现牌力评估与单主池摊牌结算。
2. 建立可回归测试，验证牌型识别与结算路径。

## 本阶段完成

1. 新增牌力评估模块：
   1. `evaluate7`（7 选 5）
   2. `compareRank`（同类牌逐项比较）
   3. 支持 10 类牌型与 A2345 wheel straight。
2. 引擎接入 showdown 结算：
   1. River 下注轮结束后自动进入 showdown 并结算单主池。
   2. all-in runout 完成后自动摊牌并结算。
   3. 平局分池与 odd chip（Button 左侧优先）已实现。
3. 折叠直胜路径补齐派奖：仅剩一人时直接派奖，不再只改 phase。
4. 补充测试：
   1. 新增 `handEvaluator` 测试，覆盖 10 类牌型识别与 kicker 比较。
   2. 引擎新增单主池 showdown 结算测试。
   3. 原有测试同步增强（fold 直胜含筹码校验）。

## 验证结果

1. `npm test` 通过（10/10）。
2. `npm run build` 通过。

## 阶段结论

1. Phase B 验收目标已达成。
2. 下一阶段进入 Phase C：边池与 reopen 完整规则。

# 2026年03月18日03时12分40秒 阶段日志

## 本阶段目标

1. 推进 Phase C：补齐边池、reopen、uncalled return 规则。
2. 通过自动化测试锁定高风险规则边界。

## 本阶段完成

1. Reopen 规则落地：
   1. 新增 `lastActionToAmountThisStreet` 状态字段。
   2. `getLegalActions` 按“新增压力是否达到完整加注”控制是否可再次 raise。
2. 结算链路升级：
   1. 实现未跟注返还（`uncalledReturn`）。
   2. 实现边池分层构建（主池 + 侧池）。
   3. 按边池逆序结算并支持每池 odd chip 分配。
3. `PAYOUT` 动作可在 `SHOWDOWN` 阶段主动触发结算。
4. 结算后状态一致性修正：派奖后自动清理 `isAllIn` 标记，避免不变量冲突。
5. 回归测试新增：
   1. 单次 short all-in 不重开加注测试。
   2. 多次 short all-in 累计重开测试。
   3. side pot + uncalled return 结算测试。

## 验证结果

1. `npm test` 通过（13/13）。
2. `npm run build` 通过。

## 阶段结论

1. Phase C 验收目标已达成。
2. 下一阶段进入 Phase D：多手循环与稳定性测试。

# 2026年03月18日03时14分54秒 阶段日志

## 本阶段目标

1. 推进 Phase D：把单手流程升级为可持续多手循环。
2. 建立连续运行稳定性回归测试。

## 本阶段完成

1. Store 循环能力增强：
   1. 在 `PAYOUT` 阶段自动尝试开新手。
   2. 支持 AI 流程跨手运行直到再次轮到 Hero 或牌局结束。
2. 引擎不变量修正：
   1. 兼容 short blind / short big blind 场景下 `currentBet` 与 `streetCommit` 的关系。
3. 连续牌局测试落地：
   1. 新增“50 手连续运行”自动化测试。
   2. 覆盖每手发牌、下注、结算与筹码守恒校验。

## 验证结果

1. `npm test` 通过（14/14）。
2. `npm run build` 通过。

## 阶段结论

1. Phase D 验收目标已达成。
2. 下一阶段进入 Phase E：UI 反馈与动画联动。

# 2026年03月18日03时21分13秒 阶段日志

## 本阶段目标

1. 推进 Phase E：先完成 UI 的行为反馈与错误提示。
2. 将 eventQueue 接入轻量动画触发，形成可感知回合反馈。

## 本阶段完成

1. Seat 动作回显：
   1. 新增座位 action badge（Fold/Call/Raise/All-in 等）。
2. 全局反馈：
   1. 新增顶部 action toast（动作、翻牌发牌、派奖提示）。
3. 错误提示：
   1. UI 接入 `lastEngineError` 并展示可读错误信息。
4. 事件动画触发：
   1. `BOARD_DEALT` 触发公共牌区入场动画。
   2. `ACTION_APPLIED` / `POT_AWARDED` 触发底池条 pulse 动画。

## 关键变更文件

1. `src/components/table/TableScene.vue`
2. `src/components/table/SeatView.vue`
3. `src/components/table/PotBar.vue`
4. `src/components/table/BoardCards.vue`

## 验证结果

1. `npm test` 通过（14/14）。
2. `npm run build` 通过。

## 阶段结论

1. Phase E 已完成“基础反馈层”。
2. 下一步聚焦：筹码飞行动画与事件消费闭环（避免 event 积压）。

# 2026年03月18日03时34分38秒 阶段日志

## 本阶段目标

1. 完成 Phase E 关键缺口：筹码飞行动画。
2. 补齐 UI 事件消费闭环，避免 event 队列持续增长。

## 本阶段完成

1. 新增 `ChipFlyLayer` 组件：
   1. 支持下注入池（seat -> pot）飞行动画。
   2. 支持派奖到座位（pot -> winners）飞行动画。
2. `TableScene` 动画联动增强：
   1. `ACTION_APPLIED` 且金额 > 0 时触发下注飞筹码。
   2. `POT_AWARDED` 时按赢家触发派奖飞筹码。
3. 事件消费策略落地：
   1. Store 新增 `consumeEvents()`。
   2. UI 侧改为批量消费事件，避免积压。

## 关键变更文件

1. `src/components/table/ChipFlyLayer.vue`
2. `src/components/table/TableScene.vue`
3. `src/stores/tableStore.ts`

## 验证结果

1. `npm test` 通过（14/14）。
2. `npm run build` 通过。

## 阶段结论

1. Phase E 的核心交付已完成。
2. 下一阶段进入 Phase F：交付前稳定化与回归矩阵。

# 2026年03月18日03时40分55秒 阶段日志

## 本阶段目标

1. 修复当前阻塞：`tableStore` 测试中的 `structuredClone` 异常。
2. 完成 Phase E 收口回归，进入 Phase F。

## 本阶段完成

1. 根因修复：
   1. 将 `tableStore` 的 `handState` 从 `ref` 调整为 `shallowRef`。
   2. 避免将 Vue Proxy 直接传入引擎 `dispatch` 时触发 `structuredClone` 的 `DataCloneError`。
2. 回归矩阵验证：
   1. `npm test -- src/stores/__tests__/tableStore.test.ts` 通过。
   2. `npm run test:regression` 通过（引擎回归 + store 事件消费）。
3. 全量质量门槛复验：
   1. `npm test` 通过（17/17）。
   2. `npm run build` 通过。
4. Spec 回填：
   1. `GAME_SPEC.md` 已标记 Phase E 完成。
   2. Sprint 6 已切换为 Phase F 当前执行阶段。

## 关键变更文件

1. `src/stores/tableStore.ts`
2. `GAME_SPEC.md`
3. `日志.md`

## 阶段结论

1. 当前阻塞已解除，规则与 UI 主链路测试全绿。
2. 项目正式进入 Phase F：交付前稳定化与验收收口。

# 2026年03月18日03时43分23秒 阶段日志

## 本阶段目标

1. 进入 Phase F，先补齐交付前文档资产。
2. 建立可执行的手工验收矩阵，避免临交付返工。

## 本阶段完成

1. 新增交付验收矩阵文档：`DELIVERY_ACCEPTANCE_MATRIX.md`。
   1. 提供 12 个手工验收场景。
   2. 每个场景包含操作步骤、预期结果、自动化覆盖说明。
   3. 提供统一结果记录模板（Pass/Fail + 备注）。
2. 新增实现说明文档：`IMPLEMENTATION_NOTES.md`。
   1. 明确当前规则实现对齐范围。
   2. 记录已知限制与非目标范围。
   3. 给出后续扩展位优先级建议。
3. `GAME_SPEC.md` 回填：
   1. 标记 Phase F 文档交付物已落地。
   2. Sprint 6 状态更新为“文档完成，体验收口进行中”。

## 关键变更文件

1. `DELIVERY_ACCEPTANCE_MATRIX.md`
2. `IMPLEMENTATION_NOTES.md`
3. `GAME_SPEC.md`
4. `日志.md`

## 阶段结论

1. 交付前文档骨架已齐备，后续可直接按矩阵执行验收。
2. 下一步聚焦：按验收矩阵跑一轮手工验证并修 UI 体验细节。

# 2026年03月18日03时57分00秒 阶段日志

## 本阶段目标

1. 执行 Phase F 第一轮交付验收。
2. 形成可追踪的验收结果文档，锁定是否可交付。

## 本阶段完成

1. 页面级手工验收（Playwright）完成关键路径验证：
   1. 开局强制盲注与 Hero 初始可行动作状态正确。
   2. `toast`、座位 `action-badge`、`chip-flight` 动画触发正常。
   3. 验证了自动开新手（公共牌计数从 5 回到 0）。
2. 自动化门禁复验：
   1. `npm test` 通过（17/17）。
   2. `npm run build` 通过。
3. 交付结果沉淀：
   1. 新增 `DELIVERY_ACCEPTANCE_RESULTS.md`。
   2. 记录 12 项验收结果，当前为 12/12 Pass。
4. `GAME_SPEC.md` 回填：
   1. Sprint 6 更新为“首轮验收执行已完成”。
   2. 未完成项聚焦为性能/多设备视觉复核。

## 关键变更文件

1. `DELIVERY_ACCEPTANCE_RESULTS.md`
2. `GAME_SPEC.md`
3. `日志.md`

## 阶段结论

1. 当前版本已达到“规则与交互主链路可交付”状态。
2. 下一步仅需做性能与多设备视觉复核后即可封版。

# 2026年03月18日03时58分53秒 阶段日志

## 本阶段目标

1. 完成 Phase F 的性能检查项。
2. 把性能结论沉淀为可执行的优化建议。

## 本阶段完成

1. 完成构建体积检查：
   1. JS 主包约 173.11 kB（gzip 66.22 kB）。
   2. CSS 主包约 7.31 kB（gzip 2.36 kB）。
2. 完成资源体积热点识别：
   1. `bg.png` 与 `bg_.png` 各约 1.1 MB，为首要下载热点。
   2. `src/resources` 总体积约 3.4 MB。
3. 新增 `PERFORMANCE_CHECK.md`：
   1. 记录体积指标。
   2. 记录运行时观察。
   3. 给出下一步优化建议（优先背景图转 WebP/AVIF）。
4. `GAME_SPEC.md` 回填：
   1. Sprint 6 增加“性能盘点已完成”。
   2. 当前剩余项收敛为“多设备视觉复核”。

## 关键变更文件

1. `PERFORMANCE_CHECK.md`
2. `GAME_SPEC.md`
3. `日志.md`

## 阶段结论

1. Phase F 的测试、验收、性能盘点均已完成。
2. 封版前仅剩多设备视觉复核。

# 2026年03月18日04时19分35秒 阶段日志

## 本阶段目标

1. 完成多设备视觉复核（手机/平板/桌面）。
2. 修复按钮区与 Hero 手牌重叠问题并完成封版前回归。

## 本阶段完成

1. 视觉问题修复：
   1. 在 `TableScene.vue` 中新增响应式布局策略。
   2. 小屏下上移 Hero 手牌区域、下移底部操作区，消除遮挡。
2. 视口复核通过：
   1. `390x844`、`430x932`、`768x1024`、`1024x768`、`1512x857` 均通过。
   2. 均满足“按钮在桌边下方且不遮挡 Hero 手牌”。
3. 证据沉淀：
   1. 生成截图 `phase-f-mobile-390.png`、`phase-f-tablet-768.png`、`phase-f-desktop-1512.png`。
   2. `DELIVERY_ACCEPTANCE_RESULTS.md` 新增多设备复核章节。
4. 质量门禁复验：
   1. `npm test` 通过（17/17）。
   2. `npm run build` 通过。
5. Spec 回填：
   1. `GAME_SPEC.md` 已标记 Phase F 完成。
   2. 未完成项调整为“非阻塞优化”。

## 关键变更文件

1. `src/components/table/TableScene.vue`
2. `DELIVERY_ACCEPTANCE_RESULTS.md`
3. `GAME_SPEC.md`
4. `日志.md`

## 阶段结论

1. 当前版本已完成规则、交互、验收、性能与多设备视觉复核。
2. 项目达到封版条件。

# 2026年03月18日04时44分22秒 阶段日志

## 本阶段目标

1. 落地发牌动画（孔牌两轮 + 公共牌含 burn）。
2. 发牌期间锁定操作按钮，避免未发完先操作。

## 本阶段完成

1. 新增 `DealCardLayer` 动画层：
   1. 支持孔牌飞行动画（牌堆 -> 各座位）。
   2. Hero 第二张牌支持落地翻面（120ms）。
   3. 支持公共牌发牌（含 burn 牌过渡 + Flop/Turn/River 发牌）。
2. `TableScene` 事件联动升级：
   1. `CARDS_DEALT` 聚合为两轮发牌时间线。
   2. `BOARD_DEALT` 映射为 burn + board 的顺序动画。
   3. 加入时间线忙碌窗口，保证动画批次串行衔接。
3. 操作区联动：
   1. `ActionPanel` 新增 `locked`，动画期间禁用按钮。
   2. 动画结束后自动恢复按钮可用状态。
4. 验证结果：
   1. `npm test` 通过（17/17）。
   2. `npm run build` 通过。
   3. 页面快验确认：发牌动画可见，发牌期间按钮被锁定，动画结束后恢复。

## 关键变更文件

1. `src/components/table/DealCardLayer.vue`
2. `src/components/table/TableScene.vue`
3. `src/components/table/ActionPanel.vue`
4. `日志.md`

## 阶段结论

1. 发牌动画已在现有事件架构下稳定接入。
2. 下一步可继续细化动画节奏与音效（非阻塞）。

# 2026年03月18日04时54分52秒 阶段日志

## 本阶段目标

1. 将发牌起点调整为桌面正中央。
2. 确保玩家卡牌在发牌完成后才显示。

## 本阶段完成

1. 发牌起点调整：
   1. `DEAL_DECK_POINT` 改为桌面中心（50, 50）。
2. 卡牌显示时机控制：
   1. 新增 `holeCardsLocked` 状态。
   2. `HAND_STARTED` 后先隐藏玩家卡牌。
   3. 孔牌发牌动画全部结束后，才解除隐藏并显示玩家卡牌。
3. 组件联动改造：
   1. `SeatView` 新增 `hideCards` 支持。
   2. `HoleCards` 新增 `hidden` 支持。
4. 页面快验结果：
   1. 刷新后早期状态：`hole-cards is-hidden`、`cards hidden`、发牌按钮锁定。
   2. 发牌结束后：隐藏类移除，卡牌显示，按钮恢复可用。
5. 质量门禁：
   1. `npm test` 通过（17/17）。
   2. `npm run build` 通过。

## 关键变更文件

1. `src/components/table/TableScene.vue`
2. `src/components/table/SeatView.vue`
3. `src/components/table/HoleCards.vue`
4. `日志.md`

## 阶段结论

1. 发牌起点与卡牌显示时机已符合新要求。
2. 动画逻辑与交互状态保持一致，无回归。

# 2026年03月18日05时06分05秒 阶段日志

## 本阶段目标

1. 增加发牌音效与更明显的飞牌轨迹。
2. 修复“发牌起点仍偏左上”问题，确保起点为桌面正中心。

## 本阶段完成

1. 发牌定位修复：
   1. `DealCardLayer` 改为 `left/top` 百分比定位 + 中心锚点。
   2. 飞牌起点使用 `DEAL_DECK_POINT = (50, 50)`，与桌面中心对齐。
2. 动画增强：
   1. 飞牌轨迹加入中段抬升与落点回弹，视觉更明显。
3. 音效接入：
   1. `DealCardLayer` 新增 `start` 事件。
   2. `TableScene` 使用 WebAudio 在每张牌发出时触发短促发牌音。
   3. 为避免浏览器自动播放限制，音频在首次用户点击后解锁启用。
4. 快验结果：
   1. 控制台无 warning。
   2. 刷新后发牌期间卡牌隐藏，发完后显示。
5. 质量门禁：
   1. `npm test` 通过（17/17）。
   2. `npm run build` 通过。

## 关键变更文件

1. `src/components/table/DealCardLayer.vue`
2. `src/components/table/TableScene.vue`
3. `日志.md`

## 阶段结论

1. 发牌动画起点、节奏、音效三项已完成联动。
2. 当前版本满足“正中心发牌 + 发完再显牌”要求。

# 2026年03月18日05时12分30秒 阶段日志

## 本阶段目标

1. 增强发牌音效与动画稳定性。
2. 彻底修复“发牌从左上角起飞”的偶发问题，保证从桌面中心起飞。

## 本阶段完成

1. 发牌时序稳定化：
   1. TableScene 新增“布局就绪后再发牌”机制。
   2. 孔牌与公共牌都改为 requestAnimationFrame 调度，最多等待 12 帧布局稳定后执行。
   3. HAND_STARTED 时会清理待发牌队列与 RAF，避免脏状态影响下一手。
2. 中心起点兜底修复：
   1. DealCardLayer 的 left/top 改为 var(--from-x, 50%) 和 var(--from-y, 50%)。
   2. 所有关键帧坐标增加中心兜底值，避免变量未就绪时退化到 (0,0)。
3. 动画轨迹细化：
   1. 中段轨迹改为预计算 mid-x/mid-y，替代表达式计算，轨迹更稳定。
   2. 维持落点收束与轻微回弹效果。
4. 质量门禁：
   1. npm test 通过（17/17）。
   2. npm run build 通过。

## 关键变更文件

1. src/components/table/TableScene.vue
2. src/components/table/DealCardLayer.vue
3. 日志.md

## 阶段结论

1. 发牌动画已具备“布局稳定 + 中心兜底”双重保护。
2. 当前版本可保证发牌从桌面中心起飞，并保持音效联动。

# 2026年03月18日05时19分28秒 阶段日志

## 本阶段目标

1. 让开局发牌也有音效。

## 本阶段完成

1. 首局启动逻辑调整：
   1. TableScene 改为首次用户交互后再启动牌局（bootstrap）。
   2. 首次交互同时解锁音频上下文，确保开局整段发牌音效可播放。
2. 交互入口扩展：
   1. 支持 pointerdown 启动。
   2. 支持 keydown 启动。
3. 启动提示：
   1. 首屏显示“点击任意位置开始游戏”。
4. 清理与稳定性：
   1. 组件卸载时正确清理 pointerdown/keydown 监听与 bootstrap 状态。
5. 质量门禁：
   1. npm test 通过（17/17）。
   2. npm run build 通过。

## 关键变更文件

1. src/components/table/TableScene.vue
2. 日志.md

## 阶段结论

1. 开局发牌音效问题已修复。
2. 现在首局发牌会在音频可用状态下播放，避免浏览器自动播放策略拦截。
